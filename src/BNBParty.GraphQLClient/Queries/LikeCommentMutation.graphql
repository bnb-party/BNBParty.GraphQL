mutation LikeComment($commentId: Int!, $tokenId: Int!) {
  likeComment(commentId: $commentId, tokenId: $tokenId) {
    commentId
    likeCounter
    content
    createdAt
    icon
    tokenId
    userAddress
  }
}