mutation AddComment($content: String!, $icon: String, $tokenId: Int!) {
  addComment(content: $content, icon: $icon, tokenId: $tokenId) {
    commentId
    content
    createdAt
    icon
    likeCounter
    tokenId
    userAddress
  }
}