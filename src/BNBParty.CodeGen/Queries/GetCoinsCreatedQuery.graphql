query GetCoinsCreated($address: String!, $chainId: Long!) {
  getCoinsCreated(address: $address, chainId: $chainId) {
    tokenId
    tokenAddress
    partyFactoryAddress
    chainId
    makerAddress
    flpAddress
    creationTransaction
    createdAt
    Block
    offChainData {
      content
      icon
      Website
      X
      Discord
      Telegram
      likeCounter
    }
  }
}