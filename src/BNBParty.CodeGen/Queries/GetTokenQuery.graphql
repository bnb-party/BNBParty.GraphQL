query GetToken($tokenId: Int!) {
  getToken(tokenId: $tokenId) {
    tokenId
    tokenAddress
    chainId
    makerAddress
    flpAddress
    creationTransaction
    createdAt
    Block
    offChainData {
      content
      icon
      Website
      X
      Discord
      Telegram
      likeCounter
    }
  }
}